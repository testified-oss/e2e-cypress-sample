name: Lint & Test
on: [push, pull_request]
jobs:
  lint-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: bun install
    - run: npm run lint
    - run: npm run test
